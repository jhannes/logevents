/**
 * Classes to setup LogEvents with .properties-files. {@link org.logevents.config.DefaultLogEventConfigurator}
 * reads files like <code>logevents-&lt;profile&gt;.properties</code> from the classpath
 * and the file system and merges them together to create a configuration of loggers and
 * observers.
 */
package org.logevents.config;